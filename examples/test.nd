//:: implicit x: int
//:: y: &string
fn funcname => 2

//:: implicit x: &float
:: w: uint
export fn fankszyn: int => 
    mut dupa = 23
    dupa.funcname()

    let dupson = 53 * dupa / (2 + 5)

    return 0

/*
mut liczba = 0
let liczba = 0
let implicit latajaca: string = 0
mut implicit referencja_latajaca: &float = 23.4
let stringi: string = 102 * ("siema" - "elo") + 2
let dupa = (((1 + 2) + 3) + 4) + 5
let dupa = 1 * 2 + 3

let dupa = y + 2 * 3 + sum(list + 23 / 4) / x.parse() 

let dupa = fankszyn(2 + 2, siema("elo"))
let dupa = (2 + 2).fankszyn(siema("elo"))
let fankszyn_siema: uint = siema(fankszyn(2))

let siema_fankszyn: uint = siema(2).fankszyn().eloelo_320()

let dwa_siema: uint = 2.siema()
let dwa_siema: uint = siema(2)
*/