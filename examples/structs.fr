:: x: zzz
fn print => @js"console.log(x);"

let struct = {
    foo: "siema",
    bar: "elo",
    witam: 23,
}

:: x : int
fn func => 
    mut a = { 
        val: { 
            inner: x
        } 
    }

    a.val.inner = struct.witam

    print(a)

let _ = func(6)

:: arr: [int]
fn f => 
    for i in arr =>
        nieistniejaca_funkcja(i)

let _ = f(5)