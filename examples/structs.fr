use test { print } 
:: x: zzz
fn print => @js"console.log(x);"

let struct = {
    foo: "siema",
    bar: "elo",
    witam: 23,
}

fn main => 
    mut a = { val: { inner: 2 } }
    a.val.inner = struct.witam

    print(a)

let _ = main()