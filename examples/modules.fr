:: arr: []int
fn square =
    let res = arr
        .util::map(util::square)

    return res

:: arr: []int
export fn plus1 =
    let res = arr
        .util::map(util::add1)

    return res

fn main =
    square([1, 2, 3, 4, 5])
        .util::print()
    plus1([1, 2, 3, 4, 5])
        .util::print()

    // util::main()