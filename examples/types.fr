use test { 
    print as log, elo,
    siema,
} 

use types

type Siema = int

type Clean = 
    elo: Siema
    field: string
    name: string
    age: uint

export fn elo => return Clean {
    elo: 32,
    field: "elo",
    name: "witam",
    age: 23
}

fn siema => print(elo())

fn elo => print(field)