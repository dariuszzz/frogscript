export fn map(
    arr  [int]
    func fn(int -> int)
) -> [int] =
    @js("return " arr ".map(" func ")")

export fn add1(x int) -> int = return x + 1

export fn square(x int) -> int = return x * x

export fn main =
    core::print("siema")
